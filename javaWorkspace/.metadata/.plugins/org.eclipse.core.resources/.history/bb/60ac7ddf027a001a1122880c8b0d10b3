package condition.test;
/*
 * Math.random()와 함께
 * switch문에서의 break 키워드에 대해서 살펴보자.
 */
public class SwitchNoBreakTest {
	public static void main(String[] args) {
		Math.
		// Math는 라이브러리 클래스. Math에서 제공하는 모든 기능들은 Math.까지 쳤을 때 뜨는 아이콘에서 s가 보임. 
		//static한 성질. s가 붙은 애들은 미리 메모리에 올려놓은 애들=객체 생성 과정 안 해도 쓸 수 있음.

	}

}
